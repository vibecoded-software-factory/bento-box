# bento-box ↔ quickshell Fidelity Audit — 2026-07-23

> Six parallel auditors compared bento-box against quickshell upstream
> (fork point `e649d24`, upstream tip `10b439f`), plus the real consumers
> (DankMaterialShell @ `fe561f2`, dms-darwin, nigiri). Every finding cites
> file:line evidence on both sides, read directly — nothing was assumed.
> North star: AGENTS.md rule 3 — quickshell's API is the specification;
> honest documented inert stubs are allowed (rule 4), undocumented or
> false-justified ones are not.
>
> Severity: 🔴 breaks DMS binding / core contract · 🟠 visible divergence ·
> 🟡 minor / latent / doc debt.
>
> **Checklist discipline:** the list is FROZEN until finished. Items are
> marked `[x]` with a DONE date and short note; descriptions never change.
>
> **dms-darwin verdict (question answered):** NOT a hallucination. It is
> the Swift reimplementation of the daemon side of DMS's own `$DMS_SOCKET`
> protocol (DMS's sanctioned extension point, `DMSService.qml:27`) with
> native macOS backends. It belongs neither in bento (which implements
> quickshell's QML API) nor in DMS (untouchable); standalone is the correct
> architecture, recorded in PARITY-AUDIT.md:183-198. It is live: launchd
> `dev.dms` running, real subscribe traffic, every subcommand has a named
> consumer. Its findings below are maintenance, not existential.

## Checklist

### WIN — windows / layer-shell (src/mac/panel_window, src/mac/wayland vs src/wayland, src/window)

- [x] **WIN-1** 🟠 DONE 2026-07-24 (WlrLayershell is now a REAL creatable window, upstream's shape: a MacPanelWindow subclass carrying layer/namespace/keyboardFocus with upstream defaults [Top, "quickshell", None] and the documented Layer superset enum; verified live: `WlrLayershell { layer: WlrLayer.Bottom; anchors {...} }` creates a visible window with the layer driving aboveWindows; exported creatable in qmltypes with prototype MacPanelWindow) — `WlrLayershell` is not a creatable window element: bento registers it `QML_UNCREATABLE` attached-only (wayland.hpp:70-72) where upstream is a creatable `ProxyWindowBase` window (wlr_layershell.hpp:97,116-117) — `WlrLayershell {}` errors the document.
- [x] **WIN-2** 🟠 DONE 2026-07-24 (the attached object now IS the backing window, exactly upstream wlr_layershell.cpp:163-169: the PanelWindow overlay registers WaylandPanelInterface [a MacPanelInterface whose panel is a WlrLayershell, mirroring upstream's WaylandPanelInterface] and qmlAttachedProperties returns that window [null for non-panels, upstream's contract]; the old 6-property copy-shim is GONE - anchors/margins/exclusiveZone/exclusionMode/color/implicitHeight and every ProxyWindowBase property resolve through the attached object, and panel<->attached writes are the SAME state via virtual setAboveWindows/setFocusable overrides with upstream's conversions; the bento* dynamic-property channel was replaced by first-class setDesktopBackground/setExclusiveKeyboard on the panel; verified live: full-surface reads, both conversion directions, exclusiveZone write flipping mode to Normal [inherited upstream behavior]) — Attached `WlrLayershell` exposes only 6 properties; upstream's attached object IS the backing window (wlr_layershell.cpp:163-169), so `anchors`/`aboveWindows`/`focusable` and every ProxyWindowBase property are undefined through bento's shim (wayland.hpp:63-68).
- [x] **WIN-3** 🟠 DONE 2026-07-24 (upstream X11's intra-process stacking ported VERBATIM: MacPanelStack mirrors XPanelStack [per-generation visible-panel list, membership follows window visibility, destructor removes] and updateDimensions gains the exact shrink loop [same layer + same screen + panels below, Ignore skips, propagate updates the panels above] plus updateLowerDimensions propagation; the false 'macOS-impossible' claim in the header comment replaced by the honest three-way split [own-process = X11 loop, other apps = nigiri reserve-zone, macOS itself = no public API]; verified live with 5 panels: B stacks at A's zone offset 30, Ignore and different-layer panels stay at the edge, bottom panel untouched, and hiding A reflows B back to 0) — Panels do not stack against each other's exclusive zones — same-edge panels overlap; upstream X11 shrinks screenGeometry by own-process lower panels' zones with a pure intra-process loop (x11/panel_window.cpp:246-266), so the "macOS-impossible" claim in panel_window.hpp:27-32 is false for the own-process case.
- [x] **WIN-4** 🟠 DONE 2026-07-24 (WlSessionLock rewritten to upstream's shape: surface QQmlComponent* property as DefaultProperty [a nested WlSessionLockSurface auto-wraps, which is exactly how DMS writes it - verified live with DMS's pattern], locked/secure with upstream's lockStateChanged/secureStateChanged names, Reloadable base; locked write keeps the honest secure-stays-false warning) — `WlSessionLock` missing the `surface` property and its DefaultProperty is `data` instead of `surface` (wayland.hpp:255-258 vs session_lock.hpp:69-72).
- [x] **WIN-5** 🟠 DONE 2026-07-24 (constraintSize QSizeF RW + stopped() signal [fires when the source dies while live - the SCKit analogue of the compositor ending the stream] + upstream's implicit-size formula VERBATIM from view.cpp:17-30 including its odd single-axis arithmetic; the QSizeF default matches upstream's default-constructed bindable bug-for-bug; verified live: RW round-trip + signal resolves) — `ScreencopyView` missing `constraintSize` and the `stopped()` signal; no implicit size derivation from source (screencopy.hpp:24-60 vs view.hpp:41,67,88,94).
- [x] **WIN-6** 🟠 DONE 2026-07-24 (ShellScreen capture wired through ScreenCaptureKit: a QuickshellScreenInfo source resolves its SCDisplay by frame [both live in the global top-left point space] and captures the full display honoring paintCursor; verified live: hasContent true and sourceSize 2940x1912 = the screen's exact pixel size [1470x956 @2x] - the gap is closed) — `ScreencopyView` cannot capture a screen (`ShellScreen` source renders nothing) though ScreenCaptureKit can capture displays — a gap, not a platform impossibility (screencopy.hpp:25-27 vs view.hpp:21-28).
- [x] **WIN-7** 🟠 DONE 2026-07-24 (the layer ladder now maps to NSWindow levels with upstream's fullscreen semantics: Overlay = NSPopUpMenuWindowLevel + NSWindowCollectionBehaviorFullScreenAuxiliary [joins fullscreen Spaces - upstream's over-fullscreen Overlay], Top = NSStatusWindowLevel WITHOUT FullScreenAuxiliary [upstream's below-fullscreen Top], Bottom = NormalLevel-1; driven by MacPanelWindow.setOverlay from WlrLayershell.setLayer; VERIFIED against the window server's own kCGWindowLayer: Top panel 25, Overlay panel 101, Bottom panel -1; the visual over-fullscreen join pends a physical check with a fullscreen app) — `WlrLayer.Overlay` collapses to `Top` and no layer renders over fullscreen apps: `aboveWindows = layer >= Top` (wayland.cpp:25) plus missing `FullScreenAuxiliary` collection behavior (bridge.mm:93-95,132) vs upstream's Top-below/Overlay-above-fullscreen semantics (wlr_layershell.hpp:34-39).
- [x] **WIN-8** 🟡 DONE 2026-07-24 (active property [always false - nothing inhibitable] and cancelled() signal added with honest docs; verified live: both resolve) — `ShortcutInhibitor` missing `active` property and `cancelled()` signal (wayland.hpp:211-243 vs shortcuts_inhibit/inhibitor.hpp:43,60).
- [x] **WIN-9** 🟡 DONE 2026-07-24 (invented property removed - zero consumers verified by grep of DMS first; live: mon.respected === undefined) — `IdleMonitor.respected` is an invented property — upstream has no such property anywhere (wayland.hpp:129,145; grep of upstream tree).
- [x] **WIN-10** 🟡 DONE 2026-07-24 (enabled and respectInhibitors default true like upstream monitor.hpp:69-71; verified live) — `IdleMonitor` defaults inverted: `enabled` and `respectInhibitors` default false where upstream defaults both true (wayland.hpp:166-167 vs idle_notify/monitor.hpp:25-33,70-72).
- [x] **WIN-11** 🟡 DONE 2026-07-24 (default "quickshell" like upstream wlr_layershell.hpp:193; inertness documented in the header - stored so reads round-trip, forwarded nowhere; verified live) — Attached `namespace` defaults `""` (upstream `"quickshell"`) and is fully inert, forwarded nowhere — undocumented (wayland.hpp:115, wayland.cpp:30-34 vs wlr_layershell.hpp:193).
- [x] **WIN-12** 🟡 DONE 2026-07-24 (typed ExclusionMode::Enum from the byte-identical panelinterface.hpp, initial Auto(2) matching the panel's real state; write round-trips to the panel via QVariant::fromValue; verified live: initial 2, write Ignore -> panel reads 1) — Attached `exclusionMode` typed `int` with initial read 0=Normal while the panel is actually in Auto(=2) (wayland.hpp:67,118 vs wlr_layershell.hpp:112,198; panelinterface.hpp:61-70).
- [x] **WIN-13** 🟡 DONE 2026-07-24 (the attached shim is now two-way: it subscribes to the panel's own NOTIFYs and pulls state back with upstream's exact conversions [aboveWindows->Top/Bottom, focusable->OnDemand/None, wlr_layershell.cpp:136-146] plus direct copies for exclusionMode/exclusiveZone/margins; verified live: panel.aboveWindows=false -> attached layer reads Bottom, panel.exclusiveZone=33 -> attached reads 33) — Attached shim is one-way: writes to `PanelWindow.aboveWindows`/`focusable`/`exclusionMode` never reflect into attached reads; upstream's attached object shares state with the window (wayland.cpp:15-77 vs wlr_layershell.cpp:136-146).
- [x] **WIN-14** 🟡 DONE 2026-07-24 (WlSessionLockSurface rewritten to upstream's surface: contentItem [real QQuickItem so anchors.fill children resolve, upstream's structure], visible/width/height with signals, screen typed QuickshellScreenInfo* null, color defaulting white [session_lock.hpp:186], data default property reparenting visual children into contentItem, Reloadable base; verified live instantiating from the lock's surface component) — `WlSessionLockSurface`: missing `contentItem`, `color` defaults invalid instead of white, `screen` untyped and always null; `WlSessionLock` missing `lockStateChanged`/`secureStateChanged` signals and Reloadable base (wayland.hpp:291-315 vs session_lock.hpp:56-89,113-138,186).
- [x] **WIN-15** 🟡 DONE 2026-07-24 (honest inert stub added to the mac Quickshell.Wayland module mirroring upstream's attached surface [blurRegion PendingRegion* RW + NOTIFY, uncreatable, QML_ATTACHED]: values stored so bindings hold, no blur applied - no ext-background-effect-v1 on macOS and the NSVisualEffectView analogue deliberately not wired [zero DMS consumers]; verified live with upstream's exact example idiom [BackgroundEffect.blurRegion: Region {...} attaches, reads back, nulls out]; exported in qmltypes; PORT.md updated) — `BackgroundEffect` attached type absent from Quickshell.Wayland on macOS (upstream background_effect/qml.hpp:36-44).
- [x] **WIN-16** 🟡 DONE 2026-07-24 (the IOPM assertion now follows upstream's surface-lifetime contract: held only while enabled AND the bound window is set and visible - a windowless inhibitor refuses like upstream's null-window rule; DMS binds window: barWindow so its behavior is preserved; verified against pmset -g assertions ground truth: exactly 1 held with two inhibitors declared, the windowless one inert) — `IdleInhibitor` ignores the `window` association and inhibits while the window is hidden/null; upstream is active only while the bound window has a surface (wayland.hpp:171-175, wayland.cpp:83-105 vs idle_inhibit/inhibitor.hpp:33-37).
- [x] **WIN-17** 🟡 DONE 2026-07-24 (captureFrame() is now a no-op while live, upstream's documented contract - the refresh timer drives the private capture path instead; the 500ms re-screenshot cadence remains the documented macOS live model) — `captureFrame()` is not a no-op while `live` is true, and "live" is a 500ms re-screenshot poll, not a stream (screencopy.mm:136,163 vs view.hpp:49).
- [x] **WIN-18** 🟡 DONE 2026-07-24 (both hallucinated comments corrected to the truth: the exclusive-zone rule is now attributed to the X11 backend with the Wayland difference stated [Ignore -1, reverse-edge margin], and the Layer re-export justification now says what is real - upstream has NO such enum, the DMS spelling is a tolerated undefined no-op there, and the re-export is a deliberate documented superset that makes it resolve) — Comment hallucinations: exclusive-zone rule attributed to "niri/wlroots verbatim" when it matches the X11 rule (panel_window.cpp:39-58 vs wlr_layershell.cpp:18-35, x11/panel_window.cpp:102-113), and a Layer enum re-export justified by a shell idiom upstream does not support (wayland.hpp:75-85).
- [x] **WIN-19** 🟡 DONE 2026-07-24 (PORT.md gains a full Quickshell.Wayland shim section: what is real [Toplevel/ToplevelManager, ScreencopyView with its 500ms live model, window-aware IdleInhibitor], the two-way attached shim, each deliberate deviation [Background suppression, exclusive-keyboard focus steal, Overlay->Top, X11 zone rule], and every honest inert stub [session lock, IdleMonitor, ShortcutInhibitor, FocusGrab] - rule 4's PORT.md half closed for the whole module) — PORT.md documents none of the Quickshell.Wayland shim's stubs/deviations (WlrLayershell attached shim, WlSessionLock, IdleMonitor, ShortcutInhibitor, ScreencopyView, Toplevel, background suppression, exclusive-keyboard focus steal) — rule 4 requires comment AND PORT.md.

### SVCA — media / hardware services (pipewire, mpris, upower, bluetooth)

- [x] **SVCA-1** 🔴 DONE 2026-07-24 (PowerProfiles/PowerProfile/PerformanceDegradationReason/powerProfileHold ported to src/mac/upower/powerprofiles.{hpp,mm} mirroring upstream's surface exactly incl. toString's None->"Invalid" hole; NSProcessInfo backend: Low Power Mode -> PowerSaver/Balanced live via power-state notification, thermal state serious+ -> HighTemperature; honest limits documented in header + PORT.md: no public LPM setter (write logs an error; Performance guard is upstream's verbatim), hasPerformanceProfile false, holds empty; verified live vs pmset: lowpowermode 0 <-> Balanced(1), both write refusals fire; live LPM flip NOT exercised) — `PowerProfiles` singleton (and `PowerProfile`/`PerformanceDegradationReason`/hold types) entirely missing from `Quickshell.Services.UPower` — upstream registers them in the same URI (powerprofiles.hpp:20-167, CMakeLists.txt:24-33); DMS's battery UI references them; undocumented in PORT.md:153-170.
- [x] **SVCA-2** 🟡 DONE 2026-07-24 (full UPower ladder from IOKit's attestable signals: IsCharged/>=99% -> FullyCharged, AC+!charging+!charged -> PendingCharge [optimized charging held below full - the exact case the old code misreported as FullyCharged], battery+<=1% -> Empty, PendingDischarge documented unreachable; verified live: Discharging@95% on battery matches pmset exactly; the PendingCharge leg needs the machine on AC below 100% to exercise) — UPower `state` can never be Empty/PendingCharge/PendingDischarge and reports FullyCharged whenever on AC and not charging (service.cpp:143-146 vs device.hpp:23-33).
- [x] **SVCA-3** 🟠 DONE 2026-07-24 (all 10 properties added mirroring upstream declarations: uniqueId as per-player counter bumped on track change [upstream player.cpp:308], trackArtists as deprecated alias sharing trackArtist's bindable+NOTIFY, trackAlbumArtist always "" [payload has no album artist - upstream's sanctioned empty], rate read live from playbackRate + write with upstream's min/max guard verbatim [MPRIS defaults 1.0/1.0, routed to adapter speed], fullscreen/canSetFullscreen inert with upstream's refusal message, supportedUriSchemes/MimeTypes empty; all 13 new names verified registered in qmltypes; value-level check pending a playing app) — Missing MprisPlayer properties: `uniqueId`, `trackAlbumArtist`, `trackArtists`, `rate`, `minRate`, `maxRate`, `fullscreen`, `canSetFullscreen`, `supportedUriSchemes`, `supportedMimeTypes` (player.hpp:59-94 vs upstream player.hpp:99-226).
- [x] **SVCA-4** 🟠 DONE 2026-07-24 (postTrackChanged emitted after the track properties update - the after half of upstream's trackChanged ordering [player.cpp:319]; openUri() implemented for real via targeted NSWorkspace open with the player's own app - the MPRIS OpenUri semantic) — Missing `postTrackChanged` signal and `openUri()` invokable (player.hpp:117-193 vs upstream player.hpp:246,365).
- [x] **SVCA-5** 🟠 DONE 2026-07-24 (isPlaying now NOTIFYs a dedicated isPlayingChanged, emitted from updateFromSnapshot only when the boolean actually flips; verified: signal resolves in QML) — `isPlaying` NOTIFY is `playbackStateChanged`; no `isPlayingChanged` signal, so `onIsPlayingChanged:` errors on bento where it works upstream (player.hpp:74 vs upstream player.hpp:199,394).
- [x] **SVCA-6** 🟠 DONE 2026-07-24 (bVolume default 1.0 via WITH_ARGS, upstream's documented unsupported-default; compile-time init, PORT.md notes it) — `volume` defaults 0.0; upstream defaults/documents 1.0 when unsupported (player.hpp:224 vs upstream player.hpp:145,455).
- [x] **SVCA-7** 🟠 DONE 2026-07-24 (identity resolved to the app's human name via NSRunningApplication -> bundle display name on disk -> raw id fallback, new identity.mm; dbusName keeps the bundle id; live check pending a playing app) — `identity` carries the bundle identifier ("com.spotify.client"), not the human-readable name upstream's MPRIS Identity gives (service.cpp:228-230, player.cpp:36-37 vs upstream player.hpp:100-101,430).
- [x] **SVCA-8** 🟠 DONE 2026-07-24 (seek/loop/shuffle wired through the vendored adapter: setPosition -> `seek` in microseconds [MRMediaRemoteSetElapsedTime], seek(offset) = position+offset like upstream, setLoopState -> `repeat` 1/2/3, setShuffle -> `shuffle` 1/3; read side from the stream's repeatMode/shuffleMode keys; canSeek/loopSupported/shuffleSupported now true; false 'MediaRemote offers no seek' comments removed, PORT.md updated; volume stays honestly unsupported; NOT verified live end-to-end - no app was playing at fix time, transport/metadata verified earlier) — Seek/position-write/shuffle/loop/rate stubbed as "MediaRemote offers no seek" while the vendored adapter itself exports `adapter_seek` + shuffle/repeat/speed commands (player.cpp:94-129 vs mediaremote-adapter/src/adapter/seek.m:15-24, bin/mediaremote-adapter.pl:29-32) — the platform-limit justification is false; the honest-stub exemption does not hold.
- [x] **SVCA-9** 🟡 DONE 2026-07-24 (lengthSupported now latches true only once the player reports a duration; length READ falls back to position while unsupported, upstream's documented contract; positionSupported stays true with a comment - elapsed/timestamp are standard MediaRemote payload fields) — `lengthSupported`/`positionSupported` hardwired true; `length` does not fall back to `position` when no duration exists (player.cpp:31-32, player.hpp:217 vs upstream player.hpp:141-144).
- [x] **SVCA-10** 🟡 DONE 2026-07-24 (resolved as documented platform mapping, not code: MediaRemote sessions are binary and DEREGISTER instead of idling stopped, so player removal IS upstream's app-quit behavior and Stopped is unreachable - stated in player.cpp and PORT.md; inventing a stopped-heuristic would be the exact hallucination pattern this audit exists to kill) — `playbackState` never becomes `Stopped` — player is removed instead when idle (player.cpp:79-80, service.cpp:216-218 vs upstream MprisPlaybackState).
- [x] **SVCA-11** 🟡 DONE 2026-07-24 (metadata now carries mpris:length in MICROSECONDS [the property stays seconds, upstream's split] and mpris:trackid from MediaRemote's contentItemIdentifier - only keys the payload actually has, nothing invented) — `metadata` map carries only title/artist/album/artUrl; no `mpris:length`, `mpris:trackid` etc. (player.cpp:55-61 vs upstream player.hpp:150-158).
- [x] **SVCA-12** 🟠 DONE 2026-07-24 (PwLink/PwLinkGroup uncreatable + PwLinkState [pw_link_state values -2..4 and as_string strings, both verified against pipewire's own link.h/introspect.c] + creatable PwNodeLinkTracker with always-empty linkGroups, new link.{hpp,cpp}; verified live: tracker instantiates and binds the real default sink, enum values/strings exact, models stay empty; PORT.md updated) — `PwLink`, `PwLinkGroup`, `PwLinkState`, creatable `PwNodeLinkTracker` missing from `Quickshell.Services.Pipewire` — instantiating/referencing them errors at load (vs qml.hpp:175-401, link.hpp:18-21); PORT.md documents empty models but not absent types.
- [x] **SVCA-13** 🟡 DONE 2026-07-24 (full SPA channel enum value for value, verified against spa/param/audio/raw.h fetched from pipewire's repo - not from memory; upstream's toString string for string incl. N/A and 1-based Aux/Custom ranges; verified live: RearCenter=11, AuxRangeStart=4096, toString exact) — `PwAudioChannel` is a 9-value subset of upstream's full SPA channel enum (node.hpp:21-31 vs upstream node.hpp:33-84); shared values match numerically; not in PORT.md.
- [x] **SVCA-14** 🟡 DONE 2026-07-24 (preferred properties NOTIFY through upstream's defaultConfiguredAudioSink/SourceChanged signal names, emitted alongside the actual-default signals; macOS's no-pending-state equivalence documented in the header; verified live: both signals resolve as functions) — `preferredDefaultAudioSink/Source` reads the current default and reuses `defaultAudioSinkChanged` as NOTIFY — pending preference unobservable, `onPreferredDefaultAudioSinkChanged` never attaches (service.hpp:37-42 vs qml.hpp:114-121).
- [x] **SVCA-15** 🟡 DONE 2026-07-24 (PwNode.properties NOTIFYs propertiesChanged like upstream qml.hpp:307; verified live: signal resolves) — `PwNode.properties` is CONSTANT; upstream NOTIFYs `propertiesChanged` (node.hpp:137 vs qml.hpp:307,339).
- [x] **SVCA-16** 🟡 DONE 2026-07-24 (resolved as documented platform model: CoreAudio is driven through a single master scalar so volumes read flat per channel, and on a flat vector upstream's proportional redistribution and averaging are IDENTICAL - the distinction is vacuous under this backend; stated in PORT.md as the volume model, not a shortcut) — `setAverageVolume`/`setVolumes` flatten all channels to one scalar instead of proportional per-channel scaling (node.cpp:79-99 vs upstream node.cpp:445-470) — CoreAudio rationale documented in comment.
- [x] **SVCA-17** 🟠 DONE 2026-07-24 (all three added: discoverableTimeout/pairableTimeout as stored-but-inert RW quint32 with upstream defaults [0 = forever] and dedicated NOTIFYs - inert like discoverable/pairable themselves, macOS owns discoverability - and dbusPath = the synthetic "/org/bluez/hci0" consistent with the devices' BlueZ-style tokens; verified live: reads, writes round-trip 180/60, signals resolve) — Missing `BluetoothAdapter` properties `discoverableTimeout`, `pairableTimeout`, `dbusPath` (bluetooth.hpp:170-183 vs adapter.hpp:72-89).
- [x] **SVCA-18** 🟠 DONE 2026-07-24 (BluetoothAdapter.enabled now NOTIFYs a dedicated enabledChanged via a single transitionState writer that emits it only when the boolean flips; verified live in QML: typeof adapter.enabledChanged === 'function') — `BluetoothAdapter.enabled` NOTIFY is `stateChanged`; no `enabledChanged` signal — `onEnabledChanged:` errors (bluetooth.hpp:173,208-214 vs adapter.hpp:65,137).
- [x] **SVCA-19** 🟠 DONE 2026-07-24 (BluetoothDevice.connected now NOTIFYs a dedicated connectedChanged via transitionState - emitted on the Connected boundary, not on every transient; verified live in QML: typeof device.connectedChanged === 'function') — `BluetoothDevice.connected` NOTIFY is `stateChanged`; no `connectedChanged` signal (bluetooth.hpp:78,139-151 vs device.hpp:79,170).
- [x] **SVCA-20** 🟡 DONE 2026-07-24 (deviceName now NOTIFYs its own deviceNameChanged [emitted from refresh alongside nameChanged only when no alias masks it]; bonded keeps reading the paired flag [macOS pairing IS bonding - link keys stored on every pair] but carries upstream's bondedChanged, emitted wherever pairedChanged is; adapter gains upstream's adapterChanged NOTIFY [never emitted - one controller, documented]; verified live: all three signals resolve as functions, bonded==paired) — `deviceName` NOTIFYs `nameChanged`, `bonded` aliases `paired` (no `bondedChanged`), `adapter` CONSTANT vs upstream's `adapterChanged` (bluetooth.hpp:74-91 vs device.hpp:69-181).
- [x] **SVCA-21** 🟡 DONE 2026-07-24 (name write now implements upstream's alias contract [device.hpp:61-66]: a write sets an alias that name() returns, an empty string falls back to the device-provided name, deviceName stays untouched - with the honest macOS caveat documented on the property: the alias is SESSION-LOCAL because IOBluetooth has no public persistent alias store; verified live: set -> name=alias with deviceName intact, clear -> falls back) — `BluetoothDevice.name` write is a silent no-op (documented) — rename UI does nothing (bluetooth.hpp:73,105 vs device.hpp:63-66,143).
- [x] **SVCA-22** 🟡 DONE 2026-07-24 (adapterId now returns the hci-style id "hci0", exactly upstream's derivation [last segment of the DBus path, adapter.cpp:48-51] applied to the synthetic /org/bluez/hci0 path the module already used for device tokens - format-compatible with upstream instead of leaking the controller MAC; verified live) — `adapterId` returns the controller MAC; upstream returns the hci-style id (bluetooth.hpp:201 vs adapter.hpp:86-97).
- [x] **SVCA-23** 🟡 DONE 2026-07-24 (PORT.md now tells the truth bluetooth.mm implements: pair/cancelPair via IOBluetoothDevicePair with the NoInputNoOutput-agent behavior, forget via the framework's stable private remove [blueutil's call - no public unpair exists], per-device battery from the IOKit registry, discovery via IOBluetoothDeviceInquiry - and the honest inert remainder is now the correct short list [discoverable/pairable + timeouts, trusted/blocked/wakeAllowed]; the doc also records the new adapterId/dbusPath/alias semantics and the live verifications) — PORT.md contradicts the code: declares pair/forget/battery "inert" (PORT.md:214-216) while bluetooth.mm implements all three — the doc must be true (rule 4).

### SVCB — shell services (pam, polkit, notifications, systemtray, desktop entries)

- [x] **SVCB-1** 🟠 DONE 2026-07-24 (active is now WRITE setActive with upstream's documented equivalence - assigning IS start()/abort(); verified live: the write routes through the refusal path) — `PamContext.active` read-only; upstream is read-write (assigning `active:` errors) (pam.hpp:17 vs qml.hpp:31).
- [x] **SVCB-2** 🟠 DONE 2026-07-24 (PamError singleton added with upstream's values 1/2/3 and toString strings, plus the error(PamError.Enum) signal; start() now follows upstream's failed-start contract: error(StartFailed) then completed(Error); verified live) — `PamError` singleton type and `error()` signal missing entirely (vs conversation.hpp:41-59, qml.hpp:105).
- [x] **SVCB-3** 🟡 DONE 2026-07-24 (pamMessage() is now argumentless like upstream qml.hpp:109 - the message travels in the properties) — `pamMessage` signal signature invented: 4 args vs upstream's zero (pam.hpp:62-63 vs qml.hpp:109).
- [x] **SVCB-4** 🟡 DONE 2026-07-24 (PamResult is now a QML_SINGLETON class with upstream's toString, string for string from conversation.cpp:29-37; verified live) — `PamResult` is a Q_NAMESPACE without `toString()`; upstream is a singleton class with it (pam.hpp:9-14 vs conversation.hpp:18-36).
- [x] **SVCB-5** 🟡 DONE 2026-07-24 (defaults "login" and "/etc/pam.d", upstream's; verified live) — `config`/`configDirectory` defaults `""` vs upstream `"login"`/`"/etc/pam.d"` (pam.hpp:66 vs qml.hpp:130-131).
- [x] **SVCB-6** 🟡 DONE 2026-07-24 (full NOTIFY topology: configDirectoryChanged, messageIsErrorChanged, responseVisibleChanged as dedicated signals; all verified resolving in QML) — PAM NOTIFY topology diverges: no `configDirectoryChanged`/`messageIsErrorChanged`/`responseVisibleChanged` signals (pam.hpp:20-27 vs qml.hpp:45-118).
- [x] **SVCB-7** 🟡 DONE 2026-07-24 (Pam stub documented in PORT.md M4 with the honest justification: macOS ships PAM but the lock path is system-owned via dms-darwin NativeLock - the old 'No PAM on macOS' comment was false and is gone) — PAM stub not documented in PORT.md (rule 4's second half).
- [x] **SVCB-8** 🟠 DONE 2026-07-24 (PolkitAgent rewritten to upstream's exact surface: path RW defaulting "", isRegistered/isActive bindable false, flow bindable null, authenticationRequestStarted + change signals; the invented registered/registerAgent() gone; honest construction log - macOS has no polkit daemon; verified live: DMS's agent?.isActive/agent?.flow patterns resolve typed values) — `PolkitAgent` surface hallucinated: exposes `registered`+`registerAgent()` vs upstream's `path`/`isRegistered`/`isActive`/`flow`/`authenticationRequestStarted` — DMS reads `agent?.isActive`/`agent?.flow`, always undefined (polkit.hpp:9-15 vs qml.hpp:36-71).
- [x] **SVCB-9** 🟠 DONE 2026-07-24 (AuthFlow added with upstream's FULL surface - message/iconName/actionId/cookie/identities/selectedIdentity/isResponseRequired/inputPrompt/responseVisible/supplementary*/isCompleted/isSuccessful/isCancelled/failed, submit()/cancelAuthenticationRequest(), the 3 auth signals, Retainable base; Identity added with id/string/displayName/isGroup; both uncreatable like upstream, never instantiated on macOS - no flow can exist) — `AuthFlow` and `Identity` QML types missing entirely — DMS's `currentFlow.submit()` path can never function (vs flow.hpp:14-97, identity.hpp:14-35).
- [x] **SVCB-10** 🟡 DONE 2026-07-24 (fabricated Polkit singleton removed - zero consumers verified by grep of DMS and dms-darwin before deletion; verified live: typeof Polkit === undefined) — Fabricated `Polkit` singleton (`Polkit.available`) with no upstream counterpart (polkit.hpp:17-26).
- [x] **SVCB-11** 🟡 DONE 2026-07-24 (Polkit stub documented in PORT.md M4 with the honest platform justification and the singleton removal noted) — Polkit stub not documented in PORT.md.
- [x] **SVCB-12** 🟡 DONE 2026-07-24 (closed(NotificationCloseReason.Enum) signal added with upstream's doc comment - never fires on macOS since no instance exists, but the name resolves) — `Notification.closed(reason)` signal missing (notifications.hpp:142-158 vs notification.hpp:188).
- [x] **SVCB-13** 🟡 DONE 2026-07-24 (Notification now inherits Retainable like upstream notification.hpp:66-68 - DMS's notification?.Retainable/onDropped binding surface exists) — `Notification` not `Retainable` — DMS binds `notification?.Retainable`/`onDropped` (null-guarded today) (vs notification.hpp:66-68).
- [x] **SVCB-14** 🟡 DONE 2026-07-24 (NotificationServer now inherits PostReloadHook like upstream qml.hpp:24, with an empty onPostReload - nothing to re-emit, the tracked list is always empty; CORRECTION to the finding: reloadableId is a Reloadable property, NOT PostReloadHook's - upstream's server rejects it too, verified in reload.hpp, so bento now errors on it exactly like upstream would) — `NotificationServer` is a plain QObject, not Reloadable/PostReloadHook — `reloadableId` errors (vs qml.hpp:24, reload.hpp:49).
- [x] **SVCB-15** 🟡 DONE 2026-07-24 (toString fallbacks now upstream's exact strings; verified live: toString(99) returns 'Invalid notification urgency' / 'Invalid notification close reason') — `toString()` fallback strings differ ("Unknown" vs upstream's "Invalid notification urgency"/"Invalid notification close reason") (notifications.cpp:12,21 vs notification.cpp:29,38).
- [x] **SVCB-16** 🟡 DONE 2026-07-24 (SystemTrayItem now carries upstream's full NOTIFY topology - id/title/icon/status/category/tooltip*/hasMenu/onlyMenu each with its signal plus ready(); menu NOTIFYs hasMenuChanged like upstream item.hpp:119, stays QObject* as the documented honest stand-in for DBusMenuHandle; PORT.md updated) — SystemTray `menu` typed `QObject*` and stub item props CONSTANT; item lacks `ready`/`*Changed` signals (systemtray.hpp:72 vs item.hpp:119,155-165) — moot while items is empty, documented.
- [x] **SVCB-17** 🟠 DONE 2026-07-24 (macOS app discovery implemented INSIDE the upstream scan pipeline [desktopentry.cpp, Q_OS_MACOS block, ledger-documented]: one synthesized entry per .app bundle from /Applications, /System/Applications(+Utilities), ~/Applications via CFBundle - id = bundle identifier [= the app_id nigiri reports, so byId() resolves toplevels], name = display name, icon = absolute .icns path [Qt icns plugin renders it; asset-catalog apps get empty, not fake], exec = open <bundle>; QDir::Hidden|System needed because cryptex app links [Safari] carry the hidden attribute and plain QDir misses them - found empirically; XDG entries still override on id conflict; verified live: 89/89 bundles, byId(com.apple.Safari) -> Safari with real icns + exec, heuristicLookup works; PORT.md table claim corrected) — DesktopEntries scans only XDG paths, so the index is empty on a stock Mac — launcher/dock/icons silently degrade; no macOS discovery (/Applications, NSWorkspace) and PORT.md:34 calls it "portable" without saying the index is empty in practice (desktopentry.cpp:483-503; DMS Controller.qml:1664, AppsDock.qml:588, DMSShellIPC.qml:78).

### CORE — portable divergence, io/ipc, startup

- [x] **CORE-1** 🔴 DONE 2026-07-24 (resolution: the fix STAYS - bento's nigiri event socket needs the redial - but it is now an explicit upstream-destined patch, not a silent divergence: marker comment in socket.cpp and a new 'Portable-tree divergence ledger' section in PORT.md listing every out-of-src/mac hunk with its justification, so anything not on the list is a bug by definition) — Socket error-path cleanup changes the portable Socket contract and is not macOS-forced: bento's `onSocketError` drops the parked socket, forces `targetConnected=false`, clears buffer and emits `connectionStateChanged` on refused dials (io/socket.cpp:70-85 vs upstream 68-71) — a real upstream bug fix that belongs upstream as a patch, not silently in the fork's portable tree (rebase conflict guaranteed; observable behavior changes on all platforms).
- [x] **CORE-2** 🟠 DONE 2026-07-24 (CRASH_HANDLER now defaults OFF on APPLE with the reason in CMakeLists + PORT.md: the reporter relaunches via /proc/self/exe which cannot exist on macOS - the handler perror()s and _exit()s leaving the forked coredump child behind, worse than no handler; DMS's release install already passed OFF explicitly, now the dev build matches; until the reporter learns _NSGetExecutablePath, no handler beats a broken one) — Crash reporter silently nonfunctional on macOS (`/proc/self/exe` readlink always fails; handler `perror`s and `_exit(-1)`, coredump child left behind) and undocumented in PORT.md/AGENTS.md (crash/handler.cpp:98, CMakeLists.txt:61, launch.cpp:153-168).
- [x] **CORE-3** 🟡 DONE 2026-07-24 (upstream 10b439f 'prevent double initialization of logger' applied VERBATIM to logging.cpp - identical content that dissolves on the next rebase, noted in the ledger; drift back to zero commits) — Upstream drift: fork lacks `10b439f` "launch: prevent double initialization of logger" (upstream logging.cpp:307-310).
- [x] **CORE-4** 🟡 DONE 2026-07-24 (the purge nuance was documented in the CORE-1 ledger entry for paths.cpp: $TMPDIR files unaccessed ~3 days get reaped, a failure mode /run/user does not have - resolution is the documentation itself, the deviation is macOS-forced) — macOS runtime dir is the purgeable per-user temp dir — files unaccessed ~3 days get reaped (instance.lock/ipc.sock/log under $TMPDIR), a failure mode /run/user doesn't have; forced deviation but the purge nuance is undocumented (paths.cpp:67-78).
- [x] **CORE-5** 🟡 DONE 2026-07-24 (PORT.md ledger note added: display 'unk' on macOS, qs list collapses to one display bucket, mismatched-display machinery is dead code here - correct for a one-session OS, inherited unchanged) — Instance display is the constant `"unk"` on macOS; the mismatched-display machinery is dead code — a PORT.md note would make it honest (command.cpp:586-599, paths.cpp:446-450).

### COMP — compositor integration + DMS consumption

- [x] **COMP-1** 🟠 DONE 2026-07-24 (usingLua added, false forever with the honest doc - DMS's `Hyprland.usingLua === true` check now evaluates a real property; verified live: reads false, the === check is false) — Hyprland stub omits `usingLua`, which DMS reads (`Hyprland.usingLua === true`, HyprlandService.qml:21) — only the isHyprland short-circuit saves it (hyprland.hpp:89-127 vs ipc/qml.hpp:22).
- [x] **COMP-2** 🟠 DONE 2026-07-24 (rawEvent(HyprlandEvent) signal + HyprlandEvent type added mirroring upstream [name/data always empty, parse() runs upstream's split algorithm verbatim over the empty body]; never emitted - no event socket exists on macOS; verified live: Connections onRawEvent attaches cleanly, type exported in qmltypes) — Hyprland stub omits the `rawEvent(HyprlandEvent)` signal and `HyprlandEvent` type — DMS has five `onRawEvent` handlers, inert only because target is null (vs ipc/qml.hpp:89, connection.hpp:40-49).
- [x] **COMP-3** 🟠 DONE 2026-07-24 (I3IpcListener rewritten to upstream's contract: PostReloadHook base, ipcEvent(I3IpcEvent*) with .type/.data that DMS destructures [the invented ipcEvent(QString,QString) signature is gone], invented start()/stop() deleted [verified undefined live], subscriptions QList<QString> with dedup setter; reload warns honestly that no i3 socket exists; verified live: upstream-style onIpcEvent handler attaches cleanly) — `I3IpcListener` invents a wrong signal signature: `ipcEvent(QString,QString)` vs upstream's `ipcEvent(I3IpcEvent*)` with `.type`/`.data` that DMS destructures; also invents `start()`/`stop()` (i3.hpp:55 vs x11/i3/ipc/listener.hpp:55, connection.hpp:41-48).
- [x] **COMP-4** 🟠 DONE 2026-07-24 (WorkspaceActivated now applies niri's event-stream semantics: target workspace goes active, siblings deactivate, focus moves only when `focused` is set - flags no longer depend on nigiri's redundant follow-up snapshot, which niri upstream does not send; VERIFIED with a replayed stream carrying the lone event [old code would have left ws1 stale active=true; new code flips to false/false with target true/true and focusedWorkspace correct] plus a live check matching the real socket's ground truth exactly) — `NigiriWorkspace.active`/`focused` can go stale on `WorkspaceActivated`: only `bFocusedWorkspace` is reassigned; per-workspace flags update only from `WorkspacesChanged` snapshots (connection.cpp:124-126, workspace.cpp:18-19) — not verified live.
- [x] **COMP-5** 🟠 DONE 2026-07-24 (the contract is now documented on BOTH sides: nigiri's NiriProtocol.swift:9-13 already names bento as the kept consumer of the bare-word dialect; bento now carries a DIALECT CONTRACT note at the subscribe site + dispatch site in connection.cpp and a PORT.md paragraph stating the bare words [event-stream, action <line>, action reserve-zone] are load-bearing and must outlive bento's migration to the JSON forms - nigiri cleanup can no longer break bento silently, the contract is written where the cleanup would happen) — Two IPC dialects must coexist on one socket forever: bento speaks legacy bare-word (`event-stream`, `action <line>`, `action reserve-zone`) while DMS speaks niri JSON on the same /tmp/nigiri-msg.sock — any nigiri cleanup of the legacy dialect breaks bento silently (connection.cpp:60,248, panel_window.cpp:347; NiriService.qml:199,731).
- [x] **COMP-6** 🟠 DONE 2026-07-24 (Glue/install.sh now generates a `niri` shim into ~/.local/bin like the dms/gsettings/cava shims: `validate` runs nigiri check-config [report to stderr, exit code mirrored - DMS toasts only on nonzero], `msg [-j] <req>` forwards the niri JSON request over the socket and unwraps the reply exactly like `niri msg -j` prints it, `msg output <name> ...` returns nigiri's honest Err [macOS owns display settings] / OutputWasMissing for unknown targets; all 4 paths verified against the LIVE compositor: outputs map with logical.scale parsed the way DMS's updateDisplayScales reads it; dms-darwin README documents the shim) — DMS's niri path shells out to a `niri` CLI no audited repo provides (`niri validate`, `niri msg -j outputs`, `niri msg output`) — output enumeration/display settings silently fail unless a PATH-visible `niri` exists (NiriService.qml:110,225,1377-1414).
- [x] **COMP-7** 🟡 DONE 2026-07-24 (HyprlandWorkspace/HyprlandMonitor/HyprlandToplevel now carry upstream's FULL property+signal surface with upstream defaults [ids -1, hex address "0" bug-for-bug, null monitor/workspace/activeWorkspace, empty lastIpcObject maps and toplevels model]; HyprlandToplevel attachable like upstream with handles null forever; DMS's .monitor.name/.workspace?.id/.wayland reads all resolve typed) — Hyprland stub types are propertyless shells contra rule 4's "expose but inert" — DMS reads `.monitor.name`/`.workspace?.id`/`.wayland` behind guards (hyprland.hpp:24-51 vs workspace.hpp:21-42, monitor.hpp:20-37, hyprland_toplevel.hpp:28-49).
- [x] **COMP-8** 🟡 DONE 2026-07-24 (activeToplevel [null], requestSocketPath/eventSocketPath [empty, upstream's unset-signature behavior], monitorFor now returns typed HyprlandMonitor* - DMS's `readonly property HyprlandMonitor monitor: Hyprland.monitorFor(screen)` binding verified live; dispatch refuses with upstream's cannot-connect honesty; workspace.activate() routes through it) — Hyprland singleton missing `activeToplevel`, `requestSocketPath`, `eventSocketPath`; `monitorFor` returns untyped QObject* (hyprland.hpp:107-117 vs ipc/qml.hpp:24-55).
- [x] **COMP-9** 🟡 DONE 2026-07-24 (GlobalShortcut [PostReloadHook base, pressed/appid/name/description/triggerDescription + pressed()/released(), default appid "quickshell", warns on reload that the protocol does not exist - never registers] and HyprlandWindow [attached, opacity 1.0/visibleMask stored so bindings hold, forwarded nowhere] added under the same URI; flat registration = upstream's umbrella resolution; verified live: shortcut instantiates + warning fires, attached opacity round-trips 0.6) — `Quickshell.Hyprland` on macOS lacks `GlobalShortcut` and surface-extension types upstream exports under the same URI (vs global_shortcuts/qml.hpp:35) — latent, DMS never references them.
- [x] **COMP-10** 🟡 DONE 2026-07-24 (full upstream surface: I3 singleton gains socketPath [empty like upstream with $I3SOCK/$SWAYSOCK unset], focusedWorkspace/focusedMonitor, monitors model, refreshMonitors/refreshWorkspaces, typed findWorkspaceByName/findMonitorByName/monitorFor, rawEvent/connected signals; I3Workspace gains all 9 properties incl. num deprecated alias + activate(); I3Monitor and I3Event added with upstream defaults [scale 1, type "unknown"]; verified live: DMS's `I3.workspaces?.values.find(ws => ws.focused === true)` and typed bindings all resolve; 5 types exported in qmltypes) — I3 stub singleton missing most of upstream's surface (focusedWorkspace/monitors/socketPath/refresh*/find*/rawEvent/connected); `I3Workspace` propertyless, `I3Monitor`/`I3Event` absent (i3.hpp:22-36 vs x11/i3/ipc/qml.hpp:19-73).
- [x] **COMP-11** 🟡 DONE 2026-07-24 (decided: PUBLIC deliberate fork API, kept - Quickshell.Nigiri is the fork's compositor-integration module, the role Quickshell.Hyprland plays upstream, and it deliberately speaks niri's vocabulary instead of Hyprland's contract because nigiri IS niri-shaped [mimicking the two-socket Hyprland surface would fake a compositor that doesn't exist]; decision + consumer map [bento's MacToplevelManager internally; DMS goes over the socket + niri shim instead] written into the module header STATUS note and PORT.md M3) — `Quickshell.Nigiri` is invented public API with zero external consumers (only bento-internal MacToplevelManager uses it) and deliberately does not mimic Quickshell.Hyprland's contract — decide: internal-only, or align with the Hyprland module shape.
- [x] **COMP-12** 🟡 DONE 2026-07-24 (PORT.md gains a Quickshell.Mac section: Desktop.setWallpaper's reason for existing [bento suppresses the shell's wallpaper layer, macOS owns the desktop, something must drive the real one], its NSWorkspace backend and failure contract, its single consumer [the generated MacWallpaperBridge.qml glue - bento stays shell-agnostic], MacPanelWindow as the panel backend, and the explicit statement that importing Quickshell.Mac is deliberate fork lock-in) — `Quickshell.Mac` is invented surface the macOS glue depends on (Desktop.setWallpaper; consumed by untracked MacWallpaperBridge.qml) — deliberate fork lock-in, documented in comments only.
- [x] **COMP-13** 🟡 DONE 2026-07-24 (= WIN-5: constraintSize added there) — mac `ScreencopyView` lacks upstream's `constraintSize` (duplicate angle of WIN-5; resolve together).
- [x] **COMP-14** 🟡 DONE 2026-07-24 (active now follows upstream's contract - it 'will not change to true until the grab begins', and none can begin on macOS, so it always reads false and the write warns honestly; the old stub stored the write and reported a grab that never existed; verified live: write true -> reads false) — `HyprlandFocusGrab.active` semantics diverge: stores the write immediately vs upstream's true-only-when-grab-begins (hyprland.hpp:66-71 vs focus_grab/qml.hpp:61-68) — inert behind guards.

### DMD — dms-darwin (architecture verdict: legitimate; maintenance items)

- [x] **DMD-1** 🟠 DONE 2026-07-24 (re-ran DMS install.sh from the new location: dev.bento.plist and ~/.local/bin/dms now reference vibecoded-software-factory paths, zero stale references left; agent restarted and running (pid verified) from the new paths - the reboot time bomb is defused; release bento rebuilt in the process, now including PowerProfiles) — Stale absolute paths after the repo move: `~/Library/LaunchAgents/dev.bento.plist` and `~/.local/bin/dms:15` point at `/Users/lucassilvestri/Downloads/GitHub/DankMaterialShell/...` which no longer exists — the running bento survives, but the next crash/reboot/`dms restart`/`dms ipc` fails; re-run DMS install.sh from the new location.
- [x] **DMD-2** 🟡 DONE 2026-07-24 (the true untracked sources were install.sh/uninstall.sh - the wrapper QML files are heredoc-GENERATED build products; both scripts now versioned in dms-darwin/Glue/ with a sibling-checkout default (DMS_DIR override), deleted from the DMS tree [only regenerable QML products remain, git clean now harmless], plus a shell.qml sha256 drift guard that warns loudly when upstream changes and the embedded copy needs review; verified: Glue/install.sh ran end-to-end from the new home, agent restarted and running - and the live bar picked up all of today's bento fixes in the process) — Untracked macOS glue lives inside DMS's working tree (install.sh, uninstall.sh, shell-macos.qml, MacWallpaperBridge.qml): not a covert patch (no tracked file modified, no shadowing), but shell-macos.qml duplicates shell.qml's body (lines 49-77 vs 25-53) so upstream changes drift silently, and untracked files are vulnerable to `git clean -fd` — version them in dms-darwin or a glue repo.
- [x] **DMD-3** 🟡 DONE 2026-07-24 (comment now argues for 9 with the real gates: gamma >= 6 AND daemon-side bluetooth pairing >= 9, both served; dms-darwin build + 51 selftest checks green) — Stale comment: Server.swift:12-14 argues for apiVersion 6 while the constant is 9 (code is right, comment is wrong).
- [x] **DMD-4** 🟡 DONE 2026-07-24 (README services table now matches the live handshake [plugins, brightness, gamma, freedesktop, bluetooth] with backends and the bento/daemon bluetooth split, documents the lock/audio-tap subcommands, and gains a Glue/ section explaining the versioned shell glue and the drift guard) — README.md:17-21 understates reality ("only brightness ✅") while gamma/freedesktop/bluetooth/plugins are implemented and announced — stale docs.

## Verified faithful (so coverage is known)

- `src/window/*`, `src/windowmanager/*`, popupanchor/types: byte-identical to upstream; PanelWindow defaults exact; geometry rule matches X11 line-for-line; WlrLayer/WlrKeyboardFocus enums exact; the attached shim covers 100% of what DMS actually sets.
- `src/launch/`, `src/ipc/`, `src/io/` (except socket.cpp), `src/core/` (except the 4 declared files), `src/services/`, `src/crash/`, `main.cpp`: byte-identical; config resolution, single-instance, `qs ipc` wire protocol unmodified.
- UPowerDevice surface + enums exact (percentage 0-1, healthPercentage 0-100, timeToEmpty/Full seconds); Mpris singleton + enums exact; PwNodeType flags exact; Bluetooth singleton/enums/invokables exact with honest documented inert props.
- Notifications: full server capability defaults, urgency/close-reason enums, Notification property surface and invokables exact; SystemTray enums/invokables exact (documented empty-tray platform limit).
- ToplevelManager/Toplevel matches upstream property-for-property, fed live from nigiri (activate/close/fullscreen work); every compositor type DMS references resolves — no load-time break found.
- DMS core QML pristine (only 4 untracked additions); dms-darwin protocol matches its consumer exactly, apiVersion gates satisfied; no dead machinery.
